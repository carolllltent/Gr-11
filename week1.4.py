#sprint 1
print("Dog")
print("Cat")
#The first line runs and prints the word dog 
#The second line runs and prints the word cat 
#The program ends after that

#sprint 2
print("Cat")
print("Dog")
#The order of the lines changed as the first line runs and prints the word cat instead of dog 
#The second line runs and prints the word dog and the code ends
#The order changed because the data inputted in the first and second line were switched, meaning that in the first line, it was not told to print the word "Dog" and instead told to print the word "Cat." Since computers follow the set instructions and pieces of code given, it printed the word dog after because that was the order in which it was told to run and print. 

#sprint 3 
#ver1
print("bubu")
print("bebeh")

#ver2
print("bebeh")
print("bubu")

#In the first version, the first line runs and prints the word bubu and then the second line runs and prints the word bebeh. Then, the code ends. Compared to the second version, the first line runs and prints the word bebeh and the second line runs and prints the word bubu. Then, the code ends, same as the first. The output is different mainly because in the second version, the word bebeh is printed first and bubu follows after, comapred to the first version in which it does the opposite. The reasoning for this is that the order of the words were changed and the computer cannot input or fill in the fact that version one prints bubu first, it only follows the instruction and code given. 

name = input("Enter your name: ")
print(name)
#The first liner runs and asks the user to input their name, by pausing the program temporarily. Once the user inputs their name, a new value is assigned to name and will print that said new value. In this case, the program asks the user for a response, it pauses and waits until it recieves a response, and the name given is stored as a value in the variable name, then that gets printed. 

#I predict that the program will run and ask the user to enter a city - preferrably the one that they live in. Then the program will pause and wait for the user to give a repsonse before continuing to run, after the response is stored as a value under the variable city and it will print "You live in" and the city that the user inputted. 
city = input("Enter a city: ")
print("You live in")
print(city)
#The output appears that way because it uses the input function to pause the code to ask the user something in which this is a response that they will input. Then this response was stored in the variable city, using this, the program ran and printed "You live in" due to the print function and then printed the value of the city through the same function but using stored values that the user inputted. 

food = input("Enter a food: ")
print(food)
print(food)
print(food)
#It prints multiple times, because the print function, designed to output information, is repeated to print the stored value of the variable food, three times. In the code, the line "print("food")" is repeated multiple times, so as the program runs, the computer listens to the code and prints that same function. 

x = input("Say something: ")
print("You said:")
print(x)
#The new output goes as follows, for the first line, it runs and and asks the user to say something and type it out, then the program pauses and waits for a response from the user. After getting a response, the program will input that value and store it as the variable x. Then the second line will run and print, you said, through the print function in which is displays an already given output within the code, in this case it is "You said" and then it will finally run and print the variable x through the stored values. 

